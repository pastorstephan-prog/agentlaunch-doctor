# Support

Use GitHub Issues for reproducible bugs and feature requests. Include the macOS version, AgentLaunch Doctor version, a synthetic plist reproducer, and `--strict` output only.

Never post real tokens, environment values, private log content, full home-directory paths, or an unredacted local report.

The project provides no individual setup service, remote access, repair guarantee, or service-level agreement.
